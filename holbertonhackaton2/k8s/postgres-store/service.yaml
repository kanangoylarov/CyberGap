apiVersion: v1
kind: Service
metadata:
  name: postgres-store
  namespace: honeypot
  labels:
    app: postgres-store
spec:
  type: ClusterIP
  selector:
    app: postgres-store
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
