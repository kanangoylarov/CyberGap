apiVersion: v1
kind: Service
metadata:
  name: store-frontend
  namespace: honeypot
  labels:
    app: store-frontend
spec:
  type: ClusterIP
  selector:
    app: store-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
