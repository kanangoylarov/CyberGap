apiVersion: v1
kind: Service
metadata:
  name: admin-api
  namespace: honeypot
  labels:
    app: admin-api
spec:
  type: ClusterIP
  selector:
    app: admin-api
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
