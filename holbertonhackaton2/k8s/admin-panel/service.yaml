apiVersion: v1
kind: Service
metadata:
  name: admin-panel
  namespace: honeypot
  labels:
    app: admin-panel
spec:
  type: ClusterIP
  selector:
    app: admin-panel
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
