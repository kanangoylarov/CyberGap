apiVersion: v1
kind: Service
metadata:
  name: store-backend
  namespace: honeypot
  labels:
    app: store-backend
spec:
  type: ClusterIP
  selector:
    app: store-backend
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
