apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: honeypot
  labels:
    app: gateway
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
