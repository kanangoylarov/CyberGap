require('dotenv').config();

const app = require('./app');
const config = require('./config');
const { initDatabase } = require('./config/database');
const { isClaudeConfigured } = require('./config/claude');

// ─── DATABASE INIT ──────────────────────────────────────────────────────────
initDatabase();

// ─── START ──────────────────────────────────────────────────────────────────
app.listen(config.port, () => {
  console.log(`\n╔══════════════════════════════════════════════════╗`);
  console.log(`║  GRC Intelligence  •  http://localhost:${config.port}      ║`);
  console.log(`╠══════════════════════════════════════════════════╣`);
  console.log(`║  Database : SQLite (db/grc.db)                  ║`);
  console.log(`║  Claude   : ${isClaudeConfigured() ? 'Connected' : 'Not configured (add API key)'}      ║`);
  console.log(`╚══════════════════════════════════════════════════╝\n`);
});
